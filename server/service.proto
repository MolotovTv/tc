syntax = "proto3";
package server;

import "github.com/molotovtv/tc/tc/build.proto";

message ProjectEnv {
  string Project = 1;
  string Env = 2;
}

service TCService {
  // Sends a greeting
  rpc LastBuild(ProjectEnv) returns (tc.Build) {
  }
}